# [Claude Code 작업 지시서] Field-Insight — 현장 이슈 접수·AI 구조화·전문가 판단·회신 루프 플랫폼

- 기획자: 정광호 (Padlet 게시자: Park Bong Hyun, 첨부 `Field-Insight_시스템_개발계획서.md` + 기획 시각화 이미지 `Field_Insight_R1.png`)
- 출처: 생성형 AI 업무자동화 전문가과정 [1차수] Padlet → "프로젝트 기획서" 섹션
- 프로젝트 유형: 멀티롤 웹 애플리케이션(접수자/전문가/관리자) + 규칙 기반 Mock AI 엔진 + 상태머신 + 회신 루프
- 관련 게시물: Day01 프로젝트 기획의 "현장문제 분석지원 시스템", "정광호_필드인사이트 시스템"

---

## 0. Claude Code에게 — 작업 방식

이 기획서는 수강생 기획서 중 유일하게 **개발 착수용 시스템 사양서 수준**으로 작성되어 있다(설계 원칙 DP-1~9, 기능 요구사항 FR-01~27, 화면 C/E/A/K, 데이터 모델, 상태머신, MVP 범위, KPI까지). 따라서 별도 해석 없이 **부록의 원문을 그대로 사양서로 사용**하고, 아래 지침만 추가로 따른다.

1. **범위는 MVP-1만** (원문 17장): 정비 Domain, 텍스트 입력, 전체 루프 1회전. FR 표에서 MVP 열이 `1`인 항목만 구현한다. `2/4`는 구조만 남기고 Mock 처리.
2. **Phase 1 아키텍처**(원문 16장)로 구현: 브라우저 + 로컬 JavaScript + **Mock AI / Rule Engine** + Local Storage. 외부 LLM API 호출 없이 동작해야 한다. (LLM 어댑터 인터페이스만 정의해 두고 Mock 구현으로 채운다.)
3. 원문이 "Phase 1에서도 실제 스키마로 구현해야 한다"고 못박은 4가지 — `evidence_ref`, `confidence`, `Issue 상태머신`, `회신 루프` — 는 절대 생략하지 않는다.
4. 원문 19장 "개발 착수 순서"를 따른다. 특히 순서 1~2(Domain→Scenario→Requirement→Question 관계 정의, 정비 Requirement/Question Matrix)는 **코드가 아니라 데이터 파일(JSON/YAML)** 로 먼저 만든다. 7.2의 MNT-01~12 표와 판별력 가중치를 그대로 옮긴다.
5. 질문 선정은 8.2의 `DecisionImpact = 판별력 × 획득가능성 × 미확보도` 공식을 구현하고, 세션당 최대 3개, 모든 질문에 [모르겠음] 포함(8.1 P1~P5).
6. 신뢰도는 9.2의 **출처 기반 테이블**을 그대로 구현(LLM 자기보고 confidence 금지). `confidence < 0.7` 필드 중 상위 2개만 확인 화면에 강조.
7. 전문가 결론의 계통/부품은 **자유 텍스트 금지** → 샘플 부품/계통 코드 마스터(예: `SW-HYD-0412`)를 만들어 검색·선택 UI로 구현. "원인 미확정" 종결 + 사유 코드 허용.
8. 15장 End-to-End 시나리오(굴착기 HX220A, ISSUE #1024)가 **그대로 재현되는 데모 데이터**를 시드로 넣고, 이 시나리오를 E2E 테스트 케이스로 작성한다.
9. 역할은 계정 분리가 아니라 `user.roles[]` + 화면 모드 토글로 구현(3장 구현 주의).
10. 기획자가 첨부한 시각화 이미지(`Field_Insight_R1.png`)가 있으면 화면 레이아웃 참고용으로 요청한다.
11. 모호한 점은 9번 질문을 사용자에게 먼저 확인하고, 답이 없으면 원문의 기본값을 따른다.

---

## 1. 한 줄 요약

현장 사용자가 전문용어 없이 현상을 설명하면 AI가 상황별 필요 정보요건을 스스로 정하고 **판단을 바꿀 질문 3개 이내**로 Issue를 정형화하고, 전문가가 근거 점프와 AI 보조 분석을 바탕으로 **확정원인·조치·근거·재발방지 4필드**로 결론을 내리면, 이를 고객 언어로 변환해 승인 후 회신하고, **고객이 해결을 확인한 사례만 Knowledge로 축적**하는 플랫폼. (자세한 정의는 원문 1장)

## 2. MVP-1 구현 체크리스트 (원문 기준 발췌)

**화면 (14장, MVP=1)**
- C-01 입력(텍스트) + 장비 선택 / C-02 추가 질문(선택지형, 진행 표시) / C-03 선택적 확인·접수 / C-04 내 접수 목록 / C-05 답변 확인 + 해결 여부 응답
- E-01 My Queue / E-02 이슈 상세 + 원본 근거 점프 / E-03 AI 분석·유사사례 비교(Mock) / E-04 구조화 결론 입력 / E-05 고객 답변 승인

**기능 (12장, MVP=1)**: FR-02, 03, 04, 05, 06, 07, 08, 09, 10, 12, 13, 14, 15, 17, 20, 21, 22, 23, 24, 25 (FR-01은 텍스트만)

**상태머신 (5장)**: DRAFT → SUBMITTED → ASSIGNED → IN_REVIEW ⇄ PENDING_FIELD → ANSWERED → RESOLVED / REOPENED → KNOWLEDGE_READY, 그리고 MERGED / STALE / CLOSED_UNVERIFIED

**데이터 모델 (13장)**: UserInput, Intent, Requirement, Question, CollectedInformation(evidence_ref 필수), Issue, AIAnalysis(verdict A/B/C/D), ExpertOpinion(4필드 + 코드), CustomerResponse, CustomerFeedback, KnowledgeEntry

**Mock AI Rule Engine이 담당할 것**: Intent 분류(키워드 규칙), 안전 위험 감지(키워드), Requirement Schema 적용·Gap 분석, DecisionImpact 정렬, 출처 기반 confidence, 유사사례/근거 Mock 반환(A/B/C/D), 전문가 자유 서술 → 4필드 초안(규칙/템플릿), 기술 언어 → 고객 언어 변환(템플릿)

## 3. 권장 기술 스택 (제안 — 원문 Phase 1 제약 내)

- 프론트: React(Vite) 또는 순수 HTML/JS SPA. 모바일 우선(현장 사용자는 장갑 착용 → 버튼 48×48pt 이상, 8.3).
- 상태/저장: 브라우저 LocalStorage/IndexedDB(Phase 1). 데이터 접근 계층을 분리해 Phase 2에서 서버/DB로 교체 가능하게.
- 규칙 엔진: 순수 JS 모듈 + JSON 스키마 파일(`domains/maintenance.requirements.json`, `parts.master.json`).
- 테스트: 15장 시나리오 E2E(Playwright) + DecisionImpact·confidence 단위 테스트.

## 4. 산출물

1. `schema/` — Domain/Scenario/Requirement/Question JSON(정비 Domain MNT-01~12), 부품/계통 코드 마스터 샘플
2. `engine/` — Mock AI Rule Engine(인터페이스 + Mock 구현), 상태머신, confidence/DecisionImpact 계산기
3. `app/` — 접수자/전문가 화면(C-01~05, E-01~05), 역할 모드 토글
4. `seed/` — ISSUE #1024 시나리오 데이터
5. `tests/` — E2E + 단위 테스트
6. README — 실행 방법, 스키마 확장 방법(Domain 추가), Phase 2 교체 지점

## 5. 구현 단계

- Phase A: 스키마 JSON + 데이터 모델 + 상태머신 + 단위 테스트 (원문 19장 1~4, 6)
- Phase B: 접수자 흐름 C-01 → C-02 → C-03 (Gap 분석 → 질문 3개 → 선택적 확인 → Issue 생성)
- Phase C: 전문가 흐름 E-01 → E-02 → E-03 → E-04 (근거 점프, A/B/C/D, 4필드 구조화 입력)
- Phase D: 회신 루프 E-05 → C-05 (고객 언어 변환, 승인, 해결 확인, REOPEN, Knowledge 승격 후보)
- Phase E: 감사 이력(FR-25), 시나리오 E2E, README

## 6. 확인 필요 사항 (작업 전 질문)

1. 기획 시각화 이미지(`Field_Insight_R1.png`)와 원본 MD 파일을 직접 제공해 줄 수 있는가? (도표 정렬 보존용)
2. MVP-1에서 STT(음성)는 제외가 맞는가? (원문은 텍스트만 MVP-1, 음성은 MVP-2)
3. 부품/계통 코드 마스터 샘플을 제공할 수 있는가? 없으면 굴착기 선회/유압/주행 계통 샘플 20개를 임의 생성해도 되는가?
4. 데모 대상 장비 모델 목록(예: HX220A 외)?
5. UI 언어는 한국어 단일로 충분한가?
6. 관리자 화면(A-01/02)은 MVP-1에서 완전히 제외해도 되는가?

---

## 부록. 기획서 원문 (첨부 `Field-Insight_시스템_개발계획서.md` 전문)

> 주의: 아래 코드 블록 안의 ASCII 도표는 Padlet에서 텍스트를 추출하는 과정에서 **들여쓰기(정렬)가 유실**되었습니다. 내용(박스 라벨, 흐름, 상태명)은 모두 보존되어 있으며, 정렬된 원본은 Padlet 게시물의 첨부 파일에서 내려받을 수 있습니다.

# Field-Insight 시스템 개발 계획서

> **문서 성격**: 개발 착수용 시스템 사양서
> **적용 범위**: 아키텍처 · AI 엔진 · 데이터 모델 · 화면 · MVP 범위
> **착수 원칙**: 화면 개발이 아니라 `Domain → Scenario → Requirement → Question → Evidence → Structured Output → Return Loop` 관계 정의부터 시작한다.

---

## 1. 프로젝트 정의

**Field-Insight**는 현장 사용자가 전문용어 없이 음성·영상·이미지·텍스트로 현상을 설명하면,
AI가 **질문의 목적과 상황을 분석하여 해결에 필요한 정보요건을 스스로 구성**하고,
**전문가의 판단을 바꿀 수 있는 정보만 선별하여 최소한으로 질문**한 뒤 Issue를 정형화하는 플랫폼이다.

정형화된 Issue에 대해 AI는 표준·매뉴얼·과거 사례를 근거로 1차 분석을 제시하고,
전문가는 **구조화된 형식(확정원인 / 조치 / 근거 / 재발방지)**으로 결론을 확정한다.
AI는 이를 고객 언어로 재작성하고, **전문가 승인을 거쳐 고객에게 회신**한다.
고객이 해결 여부를 확인한 사례만 Knowledge로 승격된다.

> **한 문장 정의**
> **경험 → 의도 파악 → 상황별 정보요건 결정 → 최소 질문 → 구조화 → 선택적 확인 → Issue → 근거 분석 → 전문가 구조화 판단 → 고객 언어 변환 → 회신 → 해결 확인 → Knowledge 축적**

Field-Insight는 AI Chatbot도, Issue Management System도 아니다.
핵심은 **"현장 경험을 해결 가능한 정보로 변환하고, 그 결과를 다시 현장으로 되돌리는 지식 인터페이스"**이다.

### 기존 방식과의 차이

일반적인 접수 시스템은 모든 Issue에 동일한 입력 항목을 적용한다.

```text
모든 입력
↓
현상 / 발생조건 / 발생위치 / 발생시점 / 빈도 / 작업조건 …
```

Field-Insight는 **질문의 성격에 따라 필요한 정보 자체가 달라진다**고 본다.

```text
사용자 입력
↓
AI가 질문의 목적/상황 파악
↓
┌──────────────────────────┐
│ 질문 유형 및 업무 영역 판단 │
│ 정비 / 운전 / 교육 / 품질 │
│ 사용성 / 안전 / 기술문의 │
└─────────────┬────────────┘
↓
해당 분야의 정보요건 선택
↓
전문가 판단을 바꿀 정보 식별
↓
최소 질문 (최대 3개)
↓
정보 충분성 판단
↓
저신뢰 항목만 사용자 확인
↓
Issue 생성
```

따라서 핵심 AI 기능은 단순한 **Information Extraction**이 아니라
**Dynamic Requirement Identification** — *"이 질문을 해결하려면 어떤 정보가 필요한가?"* 를 먼저 판단하는 것이다.

---

## 2. 설계 원칙

개발 전 구간에서 유지해야 하는 원칙이다. 개별 기능 판단이 갈릴 때 이 원칙을 기준으로 결정한다.

| # | 원칙 | 의미 |
|---|---|---|
| **DP-1** | **원본과 해석은 분리 저장한다** | AI 생성 결과가 원본을 덮어쓰지 않는다. 사후 오해석 검증이 가능해야 한다 |
| **DP-2** | **모든 정형화 값은 근거를 갖는다** | 각 필드는 원본의 어느 지점에서 나왔는지(`evidence_ref`) 가리켜야 한다. 근거를 못 대는 값은 저장하지 않고 "미확보"로 남긴다 |
| **DP-3** | **질문은 예산이다** | 빈 칸을 채우는 것이 목적이 아니다. 전문가의 판단을 바꿀 수 있는 정보만 묻는다. 세션당 최대 3개 |
| **DP-4** | **확인은 좁혀야 작동한다** | 전체 요약 승인은 승인률 100%로 수렴하여 검증 기능을 잃는다. AI가 확신하지 못한 항목만 노출한다 |
| **DP-5** | **AI는 모른다고 말할 수 있어야 한다** | 근거 없는 답변을 생성하지 않는다. "자료 없음", "정보 부족"을 숨기지 않는다 |
| **DP-6** | **입력은 자유롭게, 저장은 구조화한다** | 전문가에게 폼 입력을 강제하면 저항이 생긴다. 말은 자유롭게 하고 AI가 구조화 초안을 만든다 |
| **DP-7** | **결과는 반드시 사용자에게 돌아간다** | 접수자가 결과를 받지 못하면 두 번째 접수는 없다. 회신은 부가 기능이 아니라 재사용률을 결정하는 핵심 루프다 |
| **DP-8** | **검증된 지식만 축적한다** | 고객이 해결을 확인하지 않은 사례를 Knowledge에 넣으면, 그것이 다음 이슈의 유사사례로 검색되며 오답을 재생산한다 |
| **DP-9** | **종결은 상태이지 삭제가 아니다** | 방치 이슈는 큐에서 내리되 데이터는 보존한다 |

---

## 3. 시스템 사용자 및 역할

| 역할 | 화면 성격 | 첫 화면 | 핵심 관심사 |
|---|---|---|---|
| **접수자** (현장 사용자/고객) | Conversation 중심 | 입력 화면 | 빨리 접수하고 **결과를 받는 것** |
| **전문가** (서비스/기술) | Evidence 중심 | **My Queue** | 지금 내가 처리할 것, 판단 근거 |
| **관리자** | Monitoring 중심 | 집계 대시보드 | 처리량 · 지연 · SLA · 품질 |
| **Knowledge 관리자** | Curation 중심 | 승격 대기 목록 | 사례 정제 · 표준자료 관리 |

**세 역할은 동일한 Dashboard를 사용하지 않는다.**
처리량 집계(24 / 8 / 12 / 7 형태)는 관리자가 보는 숫자이며, 실무 전문가의 첫 화면이 되어서는 안 된다.
전문가가 화면을 열자마자 봐야 하는 것은 **"내가 지금 처리할 것" 큐 하나**다.

> **구현 주의**
> 역할은 **사람 단위가 아니라 권한/화면 모드 단위**로 구현한다.
> 서비스 엔지니어는 접수자이면서 1차 전문가인 경우가 많다.
> → `user.roles[]` + 화면 모드 전환 토글. 계정을 분리하지 않는다.
> MVP에서는 Knowledge 관리자 권한을 관리자에 포함한다.

---

## 4. 전체 프로세스

```text
┌──────────────────────────── FIELD-INSIGHT PROCESS ─────────────────────────────────┐
│ ① 현장 입력
│    음성 / 영상 / 이미지 / 텍스트 + 장비 식별(QR·시리얼·보유장비)
│    ↓
│ ② AI 이해 Engine
│    Intent 분류 → Domain/Scenario 판정 → [안전 위험 감지 시 즉시 분기]
│    ↓
│ ③ Requirement Selector
│    상황별 필요 정보요건 생성 → Information Gap 분석
│    ↓
│ ④ Question Engine
│    판단영향도(Decision Impact) 정렬 → 상위 최대 3개 → 선택지형 질문
│    ↓
│ ⑤ Selective Confirmation
│    저신뢰 필드 1~2개만 강조 확인 / 나머지는 인라인 수정
│    ↓
│ ISSUE ──────────────► (유사 이슈 후보 연결 · SLA 타이머 시작)
│    ↓
│ ⑥ Expert AI Assist
│    표준 + 매뉴얼 + 과거사례 + 유사사례 → 근거 · AI 1차 의견 · 한계 명시
│    ↓
│ ⑦ 전문가 판단
│    음성/자유 서술 → AI가 4필드 초안 구조화 → 전문가 확정
│    [확정원인(계통/부품코드) · 조치내용 · 판단근거 · 재발방지]
│    ↓
│ ⑧ Customer Response 생성
│    기술 언어 → 고객 언어 변환 → 전문가 1-click 승인 게이트
│    ↓
│ ⑨ 고객 회신
│    진행 알림(접수됨/검토중/답변완료) + 최종 답변 푸시
│    ↓
│ ⑩ 해결 확인
│    [해결됨] / [해결 안 됨] / [다시 질문]
│    ├── 해결 안 됨 ──► REOPEN ──────────────────► ⑥ 으로 복귀
│    ↓ 해결됨
│ ⑪ Knowledge 승격 (검증된 사례만)
│    └──────────────────────► ⑥ 의 유사사례 검색 품질 향상 (피드백)
└────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 5. Issue 상태 머신

```text
DRAFT (대화 진행 중)
  ↓ 사용자 접수
SUBMITTED (SLA 타이머 시작)
  ↓ 자동/수동 배정
ASSIGNED
  ↓
IN_REVIEW ──(추가 확인 요청)──► PENDING_FIELD (고객 계측/확인 대기)
  ▲◄─────────────────────────────┘
  ↓ 전문가 확정 + 승인
ANSWERED (고객 회신 완료)
  ↓ 고객 응답
  ├─► RESOLVED ──(원인 확정 + 승인)──► KNOWLEDGE_READY (승격 후보)
  └─► REOPENED ──► IN_REVIEW 로 복귀

어느 단계에서든 ──► MERGED (유사 이슈 연결) / STALE (무응답 보류) / CLOSED_UNVERIFIED
```

---

## 6. AI 아키텍처

단일 LLM 호출이 아니라 **AI Router + Domain Schema + Question Engine + Knowledge/RAG + Expert Assist** 의 조합으로 구성한다.

```text
Input (음성/영상/이미지/텍스트 + 장비ID)
  ↓
Input Understanding — STT(word-timestamp) / Vision / OCR
  ↓
Safety Pre-Filter ──► 위험 감지 시 즉시 경고 + 긴급 라우팅
  ↓
AI Router — Intent Classifier
  ├── 정비 → Requirement Schema
  ├── 운전 → Requirement Schema
  └── 교육 … → Requirement Schema
  ↓
Information Gap 분석
  ↓
Question Engine — DecisionImpact 정렬 · Top-3 · 선택지형
  ↓
Confidence Scorer — 출처 기반 신뢰도
  ↓
Selective Confirmation
  ↓
ISSUE
  ↓
Knowledge / RAG — 표준·매뉴얼·과거사례
  ↓
Expert AI Assist — 근거 + 한계 명시 · A/B/C/D 판정
  ↓
Expert Review — 구조화 초안 → 확정
  ↓
Communication Layer — 기술언어 → 고객언어 + 전문가 승인 게이트
  ↓
Return & Verify — 회신 → 해결확인 → 승격
  ↓
Knowledge (verified) ──► RAG 품질 피드백
```

### 6.1 AI 판정 4상태

AI가 무조건 답변하도록 만들면 현장 시스템에서는 위험하다. 결과 유형을 명확히 구분한다.

| 상태 | 표기 | 전문가 화면 처리 |
|---|---|---|
| **A. 근거 충분** | 판단 가능 · 신뢰도 높음 | 근거 문서 즉시 열람 링크 |
| **B. 유사 사례 존재** | 유사도 %, 동일 여부는 전문가 확인 필요 | 사례 비교 뷰 제공 |
| **C. 관련 자료 없음** | "관련 표준 또는 유사 사례를 찾지 못했습니다" 명시 | 신규 사례 등록 유도 |
| **D. 정보 부족** | 추가 필요 정보 목록 제시 | **[고객에게 추가 확인 요청]** 버튼 |

---

## 7. Domain / Requirement / Question 정의

### 7.1 Domain 분류

```text
정비 · 운전 · 교육 · 품질 · 안전 · 기술문의 · 사용성 · 기타
```

Domain에 따라 필요한 정보가 달라진다.

| Domain | 필요 정보요건 예시 |
|---|---|
| **정비** | 발생 현상 · 발생 작업 · 발생 조건 · 재현성 · 빈도 · 예열 상태 · 엔진 RPM · 정비 이력 · 경고등 |
| **운전** | 운전 조건 · 작업 종류 · 운전자 숙련도 · 조작 레버 · 기대 반응 · 실제 반응 · 작업 환경 |
| **교육** | 교육 대상 · 숙련도 · 교육 목적 · 교육 환경 · 현재 수행 수준 · 어려움을 느끼는 행동 · 평가 기준 |

예를 들어 *"신입 운전자가 붐 조작을 어려워합니다"* 라는 입력에 대해 AI는 고장 진단 질문을 하지 않고
*"어떤 조작에서 가장 어려움을 느끼나요?"* 를 묻는다. **질문 유형에 따라 질문 Tree 자체가 달라진다.**

### 7.2 정비 Domain — Requirement / Question Matrix

각 정보요건은 **판별력**(확정 시 배제 가능한 원인 후보 수)과 **획득가능성**(현장에서 즉시 답할 수 있는지)을 갖는다.

| requirement_id | 정보요건 | 판별력 | 획득가능성 | 질문 형식 | 선택지 |
|---|---|---|---|---|---|
| MNT-01 | 발생 현상 | 5 | 상 | 자유+선택 | 충격/소음/속도저하/누유/경고등 |
| MNT-02 | 발생 작업 | 4 | 상 | 선택 | 굴착/선회/주행/적재/공회전 |
| MNT-03 | 발생 조건(동작 조합) | 5 | 상 | 선택 | 붐 하강 시/붐 상승 시/무관/모르겠음 |
| MNT-04 | 재현성 | 5 | 상 | 선택 | 항상/특정 조건에서만/가끔/모르겠음 |
| MNT-05 | 예열 상태(냉간/온간) | 5 | 상 | 선택 | 시동 직후/데워진 후/무관/모르겠음 |
| MNT-06 | 발생 빈도 | 3 | 상 | 선택 | 매번/하루 수회/주 1회 이하 |
| MNT-07 | 엔진 RPM 변화 | 4 | 중 | 선택 | 동일/낮아짐/높아짐/모르겠음 |
| MNT-08 | 최근 정비 이력 | 4 | 중 | 선택 | 있음(1개월 내)/없음/모르겠음 |
| MNT-09 | 경고등/알람 | 4 | 상 | 선택 | 없음/있음(촬영)/모르겠음 |
| MNT-10 | 유압 온도 | 3 | **하** | 질문하지 않음 | → 전문가 추가 확인 항목 |
| MNT-11 | 선회 압력 | 4 | **하** | 질문하지 않음 | → 전문가 추가 확인 항목 |
| MNT-12 | 장비 식별(모델/SN/가동시간) | 5 | **자동** | 시스템 획득 | QR / 시리얼 / 보유장비 선택 |

**두 가지 설계 포인트**

- **MNT-10/11처럼 획득가능성이 낮은 항목은 고객에게 묻지 않는다.** "정보 부족"으로 표기하고 전문가의 추가 확인 요청으로 넘긴다. 계측기를 들고 있지 않은 사람에게 계측값을 물으면 이탈한다.
- **MNT-12(장비 식별)는 질문 예산을 소모하지 않는다.** 유사사례 검색 정확도를 좌우하는 최상위 변수이지만, QR 스캔이나 보유장비 선택으로 자동 획득 가능하다.

---

## 8. Question Engine

### 8.1 질문 선정 원칙

| 원칙 | 내용 |
|---|---|
| **P1. 질문 예산** | 세션당 최대 3개 (안전 도메인은 필수 확인 항목 우선, 예산 예외) |
| **P2. 선정 기준** | 빈 필드가 아니라 **전문가의 판단을 바꿀 수 있는 정보** |
| **P3. 응답 형식** | 선택지 3~4개 기본. 자유 입력은 최후 수단 |
| **P4. 미지 처리** | 모든 질문에 **[모르겠음]** 포함. 명시적 `unknown` 값으로 저장 |
| **P5. 무료 정보** | 시스템이 자동 획득 가능한 정보는 질문하지 않는다 |

> **P2가 없으면 질문이 무한 증식한다.** 빈 칸 채우기가 목적이 되는 순간 질문 수가 늘고 사용자가 이탈한다.
> **P4는 데이터 품질 문제다.** 강제 응답은 허위 데이터를 만든다. 미응답(null)과 명시적 unknown은 다른 정보다.

### 8.2 판단영향도(Decision Impact) 산정

```text
후보 원인 집합 C = {c1, c2, c3 ...} (Domain Requirement Schema에서 도출)

각 정보요건 r 에 대하여

DecisionImpact(r) = 판별력(r) × 획득가능성(r) × 미확보도(r)

├ 판별력 : 이 값이 확정되면 후보 원인 중 몇 개를 배제할 수 있는가
├ 획득가능성 : 현장에서 즉시 답할 수 있는가 (계측 필요 항목은 감점)
└ 미확보도 : 미확보 = 1, 부분 확보 = 0.5, 확보 = 0

→ DecisionImpact 상위 N(≤3)개만 질문으로 생성
```

**구현 방침**: 판별력은 LLM 추론이 아니라 **Requirement Schema에 정수 가중치로 사전 정의**한다.
LLM 자체 산정은 재현성이 없어 튜닝이 불가능하다. 운영 후 실제 해결 사례 통계로 가중치를 갱신한다.

### 8.3 질문 UI

```text
┌─────────────────────────────────────────┐
│ 2가지만 더 확인할게요 (1/2)              │
│                                         │
│ 이 현상은 언제 발생하나요?              │
│                                         │
│ [ 항상 ]        [ 가끔 ]                │
│ [ 특정 조건 ]   [ 모르겠음 ]            │
│                                         │
│ [ 음성으로 답하기 ]   [ 건너뛰기 ]      │
└─────────────────────────────────────────┘
```

- 현장은 장갑을 끼고 작업한다. 버튼 최소 터치 영역 **48×48pt 이상**, 자유 타이핑 최소화
- 진행 표시(`1/2`)로 끝이 보이게 한다 — 무한 질문 인상 제거
- [건너뛰기]는 이탈보다 낫다. 건너뛴 항목은 `skipped`로 기록

---

## 9. 접수자 화면

### 9.1 확인 화면 — 선택적 확인

전체 요약에 `[수정] [맞습니다]`를 붙이면 사용자는 거의 모두 [맞습니다]를 누른다.
Human-in-the-loop이 이름만 남고 AI 오해석이 그대로 Issue로 확정된다.
**AI가 확신하지 못한 항목으로 주의를 좁힌다.**

```text
┌──────────────────────────────────────────────┐
│ 이렇게 이해했습니다                          │
│                                              │
│ 현상       우선회 시 간헐적 충격          ✎  │
│ 작업       굴착                           ✎  │
│ 발생 조건  붐 하강 + 우선회               ✎  │
│                                              │
│ ┌────────────────────────────────────────┐   │
│ │ ⚠ 이 항목만 확인해 주세요              │   │
│ │ 발생 빈도                              │   │
│ │ "가끔 발생" 으로 이해했습니다.         │   │
│ │ [ 맞아요 ] [ 매번이에요 ] [ 수정 ]     │   │
│ └────────────────────────────────────────┘   │
│                                              │
│ 고객 원문 "기계 전체가 한 번 흔들리는 느낌"  │
│                                              │
│ [ 접수하기 ]                                 │
└──────────────────────────────────────────────┘
```

- **강조 필드는 최대 2개.** 3개 이상이면 확인이 아니라 재작성이므로 질문 단계로 되돌린다.
- 나머지 필드는 **인라인 탭 수정**(✎). 전체 재입력을 요구하지 않는다.
- 최종 버튼은 "맞습니다"가 아니라 **"접수하기"** — 승인이 아니라 행위로 프레이밍한다.

### 9.2 신뢰도 산정

LLM 자기보고 confidence는 신뢰할 수 없으므로 **값의 출처 기반**으로 산정한다.

| 값의 출처 | confidence | 확인 대상 |
|---|---|---|
| 사용자가 명시적으로 발화 | 0.95 | 미확인 |
| 선택지 질문에 직접 응답 | 0.99 | 미확인 |
| 시스템 자동 획득(장비 식별 등) | 1.00 | 미확인 |
| 문맥에서 **추론** | 0.6 | **확인 대상** |
| 유사 사례에서 기본값 보완 | 0.4 | **확인 대상** |
| 표현이 모호/다의적 | 0.5 | **확인 대상** |

→ `confidence < 0.7` 인 필드를 판단영향도 순으로 정렬하여 **상위 2개만** 강조한다.

### 9.3 회신 및 해결 확인

| 시점 | 알림 | 내용 |
|---|---|---|
| 접수 즉시 | 푸시 | "접수되었습니다 (#1024) · 예상 회신 N시간" |
| 배정 시 | 인앱 | "담당 전문가가 확인 중입니다" |
| 추가 확인 필요 | 푸시 | "현장에서 확인이 필요한 항목이 있습니다" (계측 항목 안내) |
| 답변 완료 | 푸시 | 고객 언어 답변 + `[해결됨] [해결 안 됨] [다시 질문]` |
| 무응답 3일 | 푸시 | "해결되셨나요?" 리마인드 1회 → 이후 `CLOSED_UNVERIFIED` |

**`[해결됨]`이 눌린 사례만 Knowledge로 승격한다.** 이것이 Knowledge 신뢰도의 유일한 실증 근거다.

---

## 10. 전문가 화면

### 10.1 My Queue (첫 화면)

```text
┌──────────────────────────────────────────────────────────┐
│ 내 작업   [진행중 3] [정보대기 2] [완료 12]              │
│──────────────────────────────────────────────────────────│
│ ● #1024 우선회 시 간헐적 충격           정보충분도 92%   │
│   HX220A · SN 3421 · 3,410h                              │
│   붐 하강 + 우선회 / 간헐적                              │
│   ⏱ 4시간 경과 · SLA 잔여 20h                            │
│   🔗 유사 이슈 2건                            [ 열기 ]   │
│                                                          │
│ ● #1019 주행 시 편향                    정보충분도 71%   │
│   ⚠ 유압 온도 미확보                                     │
│   ⏱ 1일 경과                                  [ 열기 ]   │
│──────────────────────────────────────────────────────────│
│ ▸ 정보 대기 (2)   고객 응답 대기 중                      │
└──────────────────────────────────────────────────────────┘
```

- 큐 정렬: `우선순위 = f(안전등급, SLA 잔여, 정보충분도, 대기시간)`
- **정보 부족 이슈는 큐 상단이 아니라 "정보 대기" 별도 레인으로 분리한다.** 지금 처리할 수 없는 항목이 상단을 점유하면 큐가 기능을 잃는다.

### 10.2 이슈 상세 — 원본 근거 점프

전문가는 결국 음성·영상을 직접 확인한다. 근거로 바로 이동할 수 없으면 매번 처음부터 다시 듣게 되고, AI 결과를 신뢰하지 않게 된다.

```text
┌────────────────────────────────────────────────────────────┐
│ ISSUE #1024   정비 · 일반 · IN_REVIEW                      │
│ HX220A / SN 3421 / 3,410h / 2026-08-13 09:12               │
│────────────────────────────────────────────────────────────│
│ ▸ 정형화 결과                         ▸ 원본 근거          │
│ 현상    우선회 시 간헐적 충격          🔊 00:04 ▶          │
│ 작업    굴착                           🔊 00:11 ▶          │
│ 조건    붐 하강 + 우선회               🔊 01:12 ▶          │
│ 빈도    간헐적 (고객 확인됨)           💬 Q1 응답          │
│ 예열    미확보                         —                   │
│                                                            │
│ 고객 원문 "오른쪽으로 돌면 덜컹거려요"                     │
│ 첨부  🔊 음성 02:31   🎬 영상 00:47   🖼 2                 │
│────────────────────────────────────────────────────────────│
│ ▸ AI 분석   판단: △ 추가 확인 필요                         │
│   유사 Case #0832 (87%) · #0714 (73%)                      │
│   Service Manual 4-2 선회 계통 점검 기준                   │
│   한계: 유압 온도·선회 압력 미확보로 동일 원인 단정 불가   │
│   [ 근거 문서 열기 ]  [ 유사사례 비교 ]                    │
└────────────────────────────────────────────────────────────┘
```

- 모든 정형화 필드 우측에 원본 재생 지점(타임스탬프)을 노출한다.
- 링크 클릭 → 해당 시점 **-2초부터 재생**, 전사 텍스트 하이라이트.
- 영상은 프레임 구간, 이미지는 bounding box로 동일 처리.

> **선행 구현 필수**: STT는 **word-level timestamp**를 저장해야 한다.
> 문장 단위만 저장하면 필드 단위 근거 링크를 만들 수 없고, 이 스키마는 나중에 소급 적용할 수 없다.

### 10.3 전문가 결론 입력

입력은 자유롭게 받되 저장은 구조화한다. 전문가에게 4칸 폼을 직접 타이핑하게 하면 입력 저항이 발생한다.

```text
전문가 음성/자유 서술 입력
↓
AI 구조화 초안 생성
↓
┌──────────────────────────────────────────────┐
│ ▸ 확정 원인 *필수                            │
│   계통 [ 선회 유압 계통 🔍 ]                 │
│   부품 [ 선회 모터 릴리프 밸브 🔍 ]          │
│   코드 SW-HYD-0412                           │
│   □ 원인 미확정으로 종결   사유 [ ▾ ]        │
│                                              │
│ ▸ 조치 내용 *필수                            │
│   [ 예열 후 재현 확인 → 미재현 시 정상 판정 ]│
│   조치 유형 [ 점검 / 조정 / 부품교체 / 안내 ]│
│                                              │
│ ▸ 판단 근거 *필수                            │
│   ☑ Case #0832  ☑ Manual 4-2  ☑ 전문가 경험  │
│   [ 냉간 시 선회 압력 순간 상승으로 판단 ]   │
│                                              │
│ ▸ 재발 방지 선택                             │
│   [ 동절기 예열 절차 안내 ]                  │
│                                              │
│ [ 임시저장 ]              [ 결론 확정 ]      │
└──────────────────────────────────────────────┘
```

- 전문가는 AI 초안을 **확인·수정**만 한다.
- 계통/부품은 **자유 텍스트 금지**. 부품 마스터 코드 검색·선택. 코드가 없으면 집계와 학습이 불가능하다.
- **"원인 미확정" 종결을 허용한다.** 현장에서 흔한 결과이며, 강제 확정은 데이터를 오염시킨다. 단 미확정 사유 코드는 필수(정보부족 / 재현불가 / 고객 미회신 / 타 부서 이관).

> **자유 텍스트 한 칸으로 받으면 Knowledge는 검색만 되고 집계도 학습도 안 되는 텍스트 더미가 된다.**
> Field-Insight의 사업적 가치는 사실상 이 네 개 필드에 달려 있다.

### 10.4 고객 답변 승인

AI가 전문가 의견을 고객 언어로 재작성하는 과정에서 의미가 바뀔 수 있다.
(예: "예열 후 확인 필요"를 고객이 "계속 사용해도 된다"로 읽는 경우)
**발송 전 전문가 1-click 승인을 거친다.**

```text
전문가 원본
"유압 오일 온도가 낮은 상태에서 선회 압력이 순간적으로
상승해서 발생하는 현상으로 판단됩니다."

AI 고객용 재작성
"장비가 충분히 예열되지 않은 상태에서 오른쪽으로 회전할 때
순간적으로 충격이 발생할 가능성이 있습니다. 장비를 충분히
예열한 후 같은 현상이 발생하는지 먼저 확인해 주세요."

[ 수정 ]  [ 승인 후 발송 ]
```

AI는 전문가 원본을 덮어쓰지 않는다. **별도의 Customer Response로 생성·저장한다.**

---

## 11. 큐 위생 규칙

### 11.1 방치 이슈 (SLA)

| 상태 | 임계 | 조치 |
|---|---|---|
| SUBMITTED 미배정 | 24h | 관리자 알림 + 자동 배정 후보 제시 |
| IN_REVIEW 정체 | SLA 초과 | 상위자 에스컬레이션 |
| PENDING_FIELD 고객 무응답 | 72h | 리마인드 1회 → **STALE 레인으로 이동** |
| ANSWERED 후 무응답 | 72h | 해결확인 리마인드 1회 → `CLOSED_UNVERIFIED` |

**자동 종결은 하지 않는다.** 큐에서 내리되 데이터는 살린다.

### 11.2 유사 / 중복 이슈

```text
동일 equipment_ref AND 7일 이내 AND 현상 임베딩 유사도 ≥ 0.85
↓
전문가 화면에 "유사 이슈 후보" 표시
↓
전문가가 [연결] 선택 시 linked_issues[] 에 기록
```

- **자동 병합은 하지 않는다.** 다른 장비나 다른 원인의 이슈를 병합하면 유사사례 검색이 오염되며, 그 손실이 병합 이득보다 크다.
- 병합/연결은 **링크 생성이며 원본 삭제가 아니다.**

---

## 12. 기능 요구사항

### 12.1 입력 · 이해

| ID | 요구사항 | MVP |
|---|---|---|
| FR-01 | Multi-modal Input (음성/텍스트/이미지/영상/파일) | 2 (텍스트는 1) |
| FR-02 | 장비 식별 자동 획득 (QR / 시리얼 / 보유장비 선택) | 1 |
| FR-03 | Original Data Preservation — 원본과 AI 해석의 분리 저장 | 1 |
| FR-04 | Intent Classification (정비/운전/교육/품질/안전/기술문의/사용성/기타) | 1 |
| FR-05 | 안전 위험 신호 감지 시 정보수집 중단 → 경고 + 긴급 에스컬레이션 | 1 |
| FR-06 | Dynamic Requirement Generation — Domain별 정보요건 동적 선택 | 1 |
| FR-07 | Information Gap Analysis (`필요 − 확보 = Gap`) | 1 |
| FR-08 | Decision-Impact 기반 질문 생성 — 최대 3개, 선택지형, [모르겠음] 포함 | 1 |
| FR-09 | Selective Confirmation — 저신뢰 필드 최대 2개만 강조, 나머지 인라인 수정 | 1 |
| FR-10 | Issue Creation (사용자 접수 시점에 확정) | 1 |

### 12.2 전문가 지원

| ID | 요구사항 | MVP |
|---|---|---|
| FR-11 | Knowledge Retrieval (표준/매뉴얼/정비자료/과거 Issue/해결사례/유사사례) | 4 (MVP-1은 Mock) |
| FR-12 | Evidence-based AI Analysis (`의견 + 근거 + 출처 + 신뢰도`) | 1 |
| FR-13 | Unknown Handling — A/B/C/D 4상태 구분 및 명시 | 1 |
| FR-14 | Evidence Provenance — 필드 단위 원본 타임스탬프 점프 | 1 |
| FR-15 | My Queue — 역할별 화면 분리 및 큐 정렬 규칙 | 1 |
| FR-16 | Expert Feedback — 음성 또는 텍스트 자유 입력 | 2 |
| FR-17 | Structured Expert Output — 확정원인(계통·부품코드)/조치/근거/재발방지 | 1 |
| FR-18 | 유사 이슈 후보 표시 및 수동 연결 | 2 |
| FR-19 | 상태별 SLA 타이머 및 에스컬레이션 (자동 종결 없음) | 2 |

### 12.3 회신 · 축적

| ID | 요구사항 | MVP |
|---|---|---|
| FR-20 | Customer Response Generation — 기술 언어 → 고객 언어 재작성 | 1 |
| FR-21 | 발송 전 전문가 승인 게이트 | 1 |
| FR-22 | 진행 상태 알림 (접수/검토중/추가확인/답변완료) | 1 |
| FR-23 | 고객 해결 확인 — [해결됨] / [해결 안 됨] / [다시 질문] | 1 |
| FR-24 | REOPEN 처리 — 동일 Issue 이력을 유지하며 재검토 | 1 |
| FR-25 | Version / Audit Trail (전 단계 변경 이력) | 1 |
| FR-26 | Knowledge 승격 게이트 — RESOLVED + 원인확정 + 승인 사례만 | 4 |
| FR-27 | 원본 미디어 보존기간 · 접근권한 · 마스킹 정책 | 2 |

> FR-25의 이력 체인: `Original → AI Interpretation → Customer Confirmation → Expert AI Analysis → Expert Opinion → Customer Response → Customer Feedback`

---

## 13. 데이터 모델

(원문은 2열 병렬 도표. 아래는 엔티티별로 재배열한 동일 내용)

```text
UserInput
├─ input_id
├─ issue_id
├─ input_type
├─ original_uri (불변)
├─ transcript[]
│   ├─ text
│   ├─ start_ms   ← 근거 점프 키
│   ├─ end_ms
│   └─ speaker
├─ timestamp
└─ metadata (device, model, sn, hours, geo)

Requirement
├─ requirement_id
├─ domain / scenario
├─ label
├─ discriminative_weight
├─ obtainability
├─ answer_type (choice/text/measure)
├─ options[]
├─ priority
└─ validation_rule

Intent
├─ domain
├─ scenario
├─ intent
├─ confidence
└─ safety_flag

CollectedInformation
├─ requirement_id
├─ value
├─ value_state (known/unknown/skipped/pending)
├─ source_type (uttered/answered/inferred/system/default)
├─ confidence
└─ evidence_ref *필수
    ├─ input_id
    ├─ start_ms / end_ms
    └─ bbox / frame_range

Question
├─ question_id
├─ requirement_id
├─ decision_impact
├─ answer_type / options[]
├─ answer
└─ answered_at

Issue
├─ issue_id
├─ status
├─ priority / safety_level
├─ equipment_ref
├─ assignee_id
├─ sla_due_at
├─ linked_issues[]
├─ confirmed_data
├─ created_at
└─ closed_reason

AIAnalysis
├─ verdict (A/B/C/D)
├─ related_documents[]
├─ similar_cases[]
├─ evidence[]
├─ recommendation
├─ confidence
└─ limitations

ExpertOpinion
├─ original_voice_uri
├─ original_text
├─ cause_system_code *필수
├─ cause_part_code *필수
├─ cause_undetermined_reason
├─ action_type / action_detail *필수
├─ rationale_refs[] *필수
├─ prevention
└─ finalized_at

CustomerResponse
├─ technical_original
├─ simplified_response
├─ approved_by
├─ approved_at
├─ delivered_at
└─ delivery_status

CustomerFeedback
├─ issue_id
├─ result (resolved/unresolved/more_question)
├─ comment
├─ responded_at
└─ reopen_count

KnowledgeEntry
├─ source_issue_id
├─ verified (해결확인 여부)
├─ curated_by
└─ reuse_count
```

**저장 구조 원칙 (DP-1)**

```text
ISSUE
├── Customer Original (음성/영상/이미지/텍스트 원본)
├── AI Interpretation (의도/구조화 정보/추가 질문/분석)
├── Customer Confirmation (최종 확인 내용)
├── Expert AI Analysis (관련 자료/유사 사례/근거/AI 의견)
├── Expert Original (음성/텍스트)
├── Expert Final Opinion (구조화 4필드)
├── Customer Response (AI 작성 고객용 설명 + 승인 이력)
└── Customer Feedback (해결 확인)
```

---

## 14. 화면 목록

| 화면 ID | 역할 | 화면 | MVP |
|---|---|---|---|
| C-01 | 접수자 | 입력(텍스트/음성) + 장비 선택 | 1 |
| C-02 | 접수자 | 추가 질문 (선택지형, 진행 표시) | 1 |
| C-03 | 접수자 | 선택적 확인 · 접수 | 1 |
| C-04 | 접수자 | 내 접수 목록 / 진행 상태 | 1 |
| C-05 | 접수자 | 답변 확인 + 해결 여부 응답 | 1 |
| E-01 | 전문가 | My Queue | 1 |
| E-02 | 전문가 | 이슈 상세 + 원본 근거 점프 | 1 |
| E-03 | 전문가 | AI 분석 · 유사사례 비교 | 1 |
| E-04 | 전문가 | 구조화 결론 입력 | 1 |
| E-05 | 전문가 | 고객 답변 승인 | 1 |
| A-01 | 관리자 | 집계 대시보드 | 2 |
| A-02 | 관리자 | SLA · 지연 · 방치 이슈 | 2 |
| K-01 | Knowledge | 승격 대기 · 사례 정제 | 4 |

---

## 15. 업무 시나리오 (End-to-End)

**Step 1 — 접수**
굴착기 운전자가 이상 현상을 발견한다. 전문용어는 모른다.
> "붐을 내리고 오른쪽으로 돌면 가끔 덜컹거립니다."

STT 변환 → Intent 분류(정비 / 이상 현상) → 장비는 QR 스캔으로 자동 식별(HX220A, SN 3421, 3,410h).

**Step 2 — 정보요건 판단**

```text
필요   현상  조건  작업  재현성  예열  빈도  RPM  정비이력
확보    ✓     ✓     ✓     ✕      ✕     △    ✕     ✕
DecisionImpact 상위 3 → 재현성 · 예열 상태 · 최근 정비 이력
```

**Step 3 — 최소 질문 (선택지형)**
> "이 현상은 언제 발생하나요?" → [항상] [가끔] [특정 조건] [모르겠음] → **가끔**
> "시동 직후에도 발생하나요?" → [시동 직후] [데워진 후] [무관] [모르겠음] → **시동 직후**
> "최근 1개월 내 정비 이력이 있나요?" → [있음] [없음] [모르겠음] → **없음**

**Step 4 — 선택적 확인**
빈도만 confidence 0.6 → 해당 항목만 강조 확인 → 사용자 [맞아요] → **[접수하기]** → ISSUE #1024 생성

**Step 5 — 전문가 검토**
My Queue에서 #1024 진입. 각 필드 옆 타임스탬프로 원본 즉시 확인.
AI 판정 **B(유사 사례 존재)**: Case #0832(87%), Manual 4-2. 한계로 유압 온도·선회 압력 미확보 명시.

**Step 6 — 결론 확정**
전문가가 음성으로 서술 → AI가 4필드 초안 생성 → 전문가 확인 후 확정.

```text
확정 원인   선회 유압 계통 / 냉간 시 선회 압력 상승 (SW-HYD-0412)
조치 내용   예열 후 재현 확인 → 미재현 시 정상 판정 (조치유형: 안내)
판단 근거   Case #0832 + Manual 4-2 + 전문가 경험
재발 방지   동절기 예열 절차 안내
```

**Step 7 — 회신 및 승인**
AI 재작성문 검토 후 [승인 후 발송].
> "장비가 충분히 예열되지 않은 상태에서 오른쪽으로 회전할 때 충격이 발생할 가능성이 있습니다. 충분히 예열한 후 같은 작업을 다시 해보시고, 동일한 현상이 계속되면 알려주세요."

**Step 8 — 해결 확인 및 축적**
고객 [해결됨] → RESOLVED → 원인 확정 + 승인 → Knowledge 승격 → 이후 유사 이슈의 검색 근거로 재사용.

---

## 16. 보안 및 온디바이스 개발 단계

사내 보안·로컬 환경을 고려하여 단계적으로 발전시킨다. UX와 업무 프로세스를 먼저 검증하고 AI 모델은 나중에 교체한다.

| Phase | 구성 |
|---|---|
| **Phase 1** | Web Browser → Local JavaScript → **Mock AI / Rule Engine** → Local Storage |
| **Phase 2** | Local STT → Local LLM → Local Vector DB → Local Knowledge |
| **Phase 3** | Local AI + 사내 승인 Private AI + 사내 Knowledge Repository |

> **Phase 1에서도 실제 스키마로 구현해야 하는 4가지**
> `evidence_ref` · `confidence` · `Issue 상태머신` · `회신 루프`
> 이것들은 AI를 교체해도 유지되는 뼈대이며, 나중에 붙이면 전면 재작업이 된다.

**원본 미디어 정책 (FR-27)**: 음성·영상에 제3자의 얼굴이나 대화가 포함될 수 있다. 보존기간, 접근권한, 마스킹 기준을 개발 착수 전에 확정한다.

---

## 17. MVP 범위

### MVP-1 — 정비 Domain, 텍스트 입력, 전체 루프 1회전

```text
텍스트 입력 + 장비 선택
↓ 정비 Intent 판정
↓ 정비 Requirement Schema 적용
↓ Gap 분석 → DecisionImpact 상위 3개 선택지 질문
↓ 선택적 확인 (저신뢰 2개)
↓ Issue 생성 (상태머신 시작)
↓ 전문가 My Queue → 상세 (근거 점프)
↓ 구조화 결론 입력 (확정원인/조치/근거/재발방지)
↓ AI 고객언어 변환 → 전문가 승인
↓ 고객 회신 → 해결 확인
↓ Knowledge 승격 후보
```

**MVP-1에서 회신·해결확인·구조화 출력·근거 링크를 빼면 동작하는 데모는 되지만 가치 검증은 되지 않는다.**

| 단계 | 범위 |
|---|---|
| **MVP-2** | 음성 입력(STT + word timestamp), 유사 이슈 표시, SLA, 원본 보존 정책 |
| **MVP-3** | 이미지/영상 분석, 근거 bounding box / 프레임 구간 |
| **MVP-4** | Knowledge / RAG 연결, 승격 워크플로 |
| **MVP-5** | Domain 확장 (운전 / 교육 / 품질 / 안전 / 기술지원) |
| **이후** | 오프라인·저대역폭 대응(로컬 큐잉 후 지연 업로드), 다국어 |

---

## 18. KPI

AI 정확도만 측정해서는 시스템의 성패를 알 수 없다.

### 고객 측
- Issue 등록 소요 시간 / 추가 질문 횟수 / 사용자 수정률
- 전문용어 없이 등록 가능한 비율
- 회신 열람률 · 해결 확인 응답률
- **재접수율 (동일 사용자 2회 이상 접수 비율) — 시스템 생존 지표**

### AI 측
- Intent 분류 정확도 / 정보요건 판단 정확도
- **질문 효율 = 해결 기여 질문 수 ÷ 총 질문 수**
- **확인 화면 수정 발생률** (지나치게 낮으면 형식적 승인이 발생 중이라는 신호)
- 정보 누락 탐지율 / 근거 정확도 / Hallucination 발생률

### 전문가 측
- Issue 이해 시간 / 검토 시간 / 해결 시간
- **원본 재생 클릭률** (근거 링크 유효성)
- **AI 구조화 초안 채택률** (수정 없이 확정된 비율)
- 유사 사례 검색 시간

### Knowledge 측
- **검증 사례 비율 = RESOLVED 확인 사례 ÷ 전체 종결 사례**
- **원인 코드 확정률**
- 재사용률 / 유사사례 검색 성공률 / 동일 Issue 반복 감소율

---

## 19. 개발 착수 순서

| 순서 | 산출물 | 담당 | 선행 |
|---|---|---|---|
| 1 | **Domain → Scenario → Requirement → Question 관계 정의** | 기획 + 서비스 전문가 | — |
| 2 | **정비 Domain Requirement / Question Matrix** (판별력 가중치 포함) | 서비스 전문가 | 1 |
| 3 | **부품 / 계통 코드 마스터 확보** | 서비스 · 품질 | 병행 |
| 4 | 데이터 모델 확정 (`evidence_ref`, 상태머신 포함) | 개발 | 1, 2 |
| 5 | 화면별 UX Flow (C-01~05, E-01~05) | 기획 | 2, 4 |
| 6 | Mock AI Rule Engine 구현 | 개발 | 4 |
| 7 | MVP-1 WBS 및 일정 | PM | 5, 6 |

> **3번이 병목이 되기 쉽다.** 부품/계통 코드 체계가 없으면 확정원인 필드가 자유 텍스트로 퇴화하고,
> Field-Insight의 사업가치인 집계·학습이 성립하지 않는다. 착수와 동시에 병행 확보한다.

---

## 20. 한 장 요약

```text
FIELD-INSIGHT
현장의 경험을 검증된 지식으로 바꾸다

현장 사용자 (음성/영상/이미지/텍스트)
  ── 말한 그대로 ──►  AI 이해 Engine (의도→분야→정보요건, 판단을 바꿀 질문 3개)
  ◄── 선택지 질문 ──
        ↓ 저신뢰 2개만 확인
      ISSUE
        ↓
  Expert AI Assist (근거 + 한계 + A/B/C/D)
        ↓
  전문가 (My Queue) — 원본 근거 점프 → 확정원인·조치·근거·예방
        ↓
  AI 고객언어 변환 + 전문가 승인
        ↓
      회신 ──────────────► 현장 사용자
        ↓
  [해결됨] / [해결 안 됨]
     ↓            ↓
  Knowledge 승격   REOPEN
  (검증된 사례만)
     ↓
  조직의 지식자산 → 다음 이슈의 유사사례로 재사용
```

---

## 2차 고도화 요청 (기획자 정광호 현장 적용 피드백 — 원문 취지)

MVP-1 사용 후 기획자 피드백: **"위험한 현장에서 화면을 보면서 텍스트를 입력하는 것은 불편하고 안전 부담이 있다."** 아래 입력 방식의 검토·구현을 요청함 (원문 사양의 MVP-2 음성 STT / MVP-3 이미지·영상에 해당).

1. 음성 녹음 버튼 및 음성 삽입
2. 음성 녹음 내용의 자동 텍스트 변환(STT)
3. 이미지 및 영상 삽입 (영상은 용량 제한)
4. 텍스트·이미지·영상 입력 내용 기반 AI 분석
5. 분석 결과 기반 누락 정보 확인용 AI 추가 질문 도출
6. 고려사항이나 더 쉬운 대안이 있으면 함께 제시

### 구현 원칙 (1차 원칙 유지)

- DP-1: 원본(오디오/이미지/영상 Blob)은 IndexedDB 에 불변 보존, Issue 에는 media_id 참조만
- DP-2/FR-14: 음성 전사는 세그먼트 타임스탬프(`{text, start_ms, end_ms}`)로 저장 → 근거 점프에서 해당 지점부터 재생
- FR-05: 미디어 분석 위험 신호도 기존 안전 분기로 연동
- FR-27: 보존기간·접근권한·마스킹 기준은 확산 전 확정 필요 항목으로 문서화
- 기본 경로는 완전 오프라인(Mock 규칙 엔진). 외부 API(STT 정밀·비전 분석)는 **사용자가 설정에서 키를 입력한 경우에만** 호출

### 산출물

- `engine/media.js` · `engine/stt.js` · `engine/aivision.js` (+단위 테스트), `app/mediastore.js`
- C-01 음성 녹음(핸즈프리 오버레이)·실시간 STT·이미지/영상 첨부(영상 50MB 제한), C-03/E-02 첨부 표시, E-02 음성 세그먼트 근거 점프, E-03 미디어 분석 섹션, ⚙ 설정
- `docs/현장입력_고도화_검토의견.md` — 방식 비교·보존 정책·word-timestamp·쉬운 대안·안전 권고 (수업 논의 자료)
